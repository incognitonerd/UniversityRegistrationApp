package junit.com.universityregistration.services.impl;
import junit.framework.TestCase;

public class ShowAllUniversitiesServiceImplTest extends TestCase {
	/*
	 * @Test public void testSetLocation(){ Geometry g = new Geometry(); g.setLocation(new Location()); assertNotNull(g.getLocation()); assertTrue(g instanceof Geometry); }
	 */
}
